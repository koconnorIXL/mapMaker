var Dataset = function (name, colors, subOptions, pathColors, filterInfo, styleInfo) {
	this.name = name;
	this.colors = colors;
	this.subOptions = subOptions;
	this.pathColors = pathColors;
	this.filterInfo = filterInfo;
	this.styleInfo = styleInfo;
}

module.exports = Dataset;